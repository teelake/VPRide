import 'package:flutter/material.dart';

/// Brand palette — primary yellow on actions uses [secondary] for label text.
abstract final class AppColors {
  static const Color primary = Color(0xFFFFC300);
  static const Color secondary = Color(0xFF121212);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF5F5F5);
  static const Color border = Color(0xFFE6E6E6);
}
